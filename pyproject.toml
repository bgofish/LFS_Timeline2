[project]
name = "lfs_spreadsheet_editor"
version = "0.1.0"
description = "Spreadsheet style camera path editor"
dependencies = []

[tool.lichtfeld]
type = "plugin"
hot_reload = true  # Add this line to resolve the error
